const baseUrl = `http://sportapp-miso.s3-website-us-east-1.amazonaws.com`;

const adminUrl = `${baseUrl}/#`;
const adminUrls = {
    entrenamientoUrl: `${adminUrl}`,
    calendarioUrl: `${adminUrl}calendario`
};

export {adminUrls}
